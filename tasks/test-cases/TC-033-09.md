---
tc_id: TC-033-09
title: inbox_read_pandas 读旧格式（type=implement）→ 旧 handler 正常处理
req_ref: REQ-033
layer: L1
type: functional
status: ready
---

# Scenario

`inbox_read_pandas()` 能兼容读取旧格式消息（type=implement 等旧枚举值），
通过 `_inbox_read_legacy()` 路由，不因新 ATM Envelope 路由而中断。

# Steps

1. 在 `for-pandas/` 写入旧格式消息：
   ```yaml
   ---
   type: tc_complete
   req_id: REQ-033
   status: success
   summary: TC approved
   ---
   ```
2. 调用 `inbox_read_pandas`（mock tg 函数）

# Expected

- exit code 0
- `_handle_tc_complete` 被调用（旧格式 tc_complete → implement 路由）
- 消费后消息文件被删除
