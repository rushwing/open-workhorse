---
tc_id: TC-033-07
title: inbox_read_pandas 读新格式 response → 路由到正确 handler
req_ref: REQ-033
layer: L1
type: functional
status: ready
---

# Scenario

`inbox_read_pandas()` 处理 ATM Envelope 格式（type=response）消息时，
能正确路由到 `_handle_dev_complete` handler。

# Steps

1. 在 `for-pandas/` 写入新格式 response 消息文件：
   ```yaml
   ---
   message_id: msg_menglan_resp
   type: response
   from: menglan
   to: pandas
   req_id: REQ-033
   pr_number: 42
   summary: 实现完成
   status: success
   ---
   ```
2. 调用 `inbox_read_pandas`（mock tg_pr_ready）

# Expected

- exit code 0
- `tg_pr_ready` 被调用（dev_complete success 路径）
- 消费后消息文件被删除
