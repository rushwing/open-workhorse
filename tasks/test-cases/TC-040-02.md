---
tc_id: TC-040-02
title: 缺 claim commit — exit 1, ① 步标 ✗
req_ref: REQ-040
layer: L1
type: functional
status: ready
---

# Scenario

某 fixture branch 的 git log 中不包含合规的 claim commit（无 owner 变更 + status→in_progress），
脚本应 exit 1 并在报告中将该 fixture 的 ① 步标记为 ✗，② 步标记为 -（依赖 ① 通过）。

# Steps

1. mock REQ-E001 branch git log：缺少 claim commit（无 `owner: unassigned→claude_code` 行）
2. REQ-E002 / REQ-E003 mock 为合规
3. 调用 eval:compliance
4. 检查退出码和 last-run.md

# Expected

- 退出码为 1
- REQ-E001 行：① 列显示 ✗，② 列显示 -（因 ① 失败跳过），③ 列显示 -
- score 列显示 0/3
- REQ-E002 和 REQ-E003 仍正常显示各自结果
