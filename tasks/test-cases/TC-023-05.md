---
tc_id: TC-023-05
title: tc_complete status=blocked iteration=2 触发 tg_decision
req_ref: REQ-023
layer: L1
type: functional
status: ready
---

# Scenario

`tc_complete` 消息 status=blocked，iteration=2（达到上限），
应调用 `tg_decision` 升级决策而不再路由到 Huahua。

# Steps

1. 创建 `inbox/for-pandas/` 消息：
   `type: tc_complete, req_id: REQ-021, status: blocked, blocking_reason: still missing, iteration: 2`
2. Mock `tg_decision` 为打印函数
3. 调用 `inbox_read_pandas`
4. 断言 mock `tg_decision` 被调用
5. 断言 `inbox/for-huahua/` 无新文件

# Expected

- `tg_decision` 被调用，消息包含 "2轮" 或 "超过"
- `for-huahua/` 无新 tc_design 文件
