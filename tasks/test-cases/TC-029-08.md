---
tc_id: TC-029-08
title: draft → review_ready 合法流转及 Pandas state-as-lock 说明存在于 requirement-standard.md §6.2
req_ref: REQ-029
layer: L1
type: functional
status: ready
---

# Scenario

`requirement-standard.md` v0.4 的 §6.2 合法流转表应包含 `draft → review_ready` 和 `review_ready → req_review` 流转，并说明 Pandas 使用 state-as-lock 原子 commit 防止重复认领。

# Steps

1. 执行：`grep -q 'review_ready' harness/requirement-standard.md`
2. 检查退出码
3. 可选：`grep -q 'state-as-lock\|state_as_lock\|原子' harness/requirement-standard.md` 以验证 state-as-lock 说明

# Expected

- 退出码：0
- 文档含 `review_ready` 相关流转描述
- 文档含 state-as-lock 或 "原子 commit" 说明（Pandas 防重复认领机制）
