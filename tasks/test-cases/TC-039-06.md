---
tc_id: TC-039-06
req_id: REQ-039
title: "harness.sh cmd_implement：EXISTING_BRANCH 非空时 prompt 含 PR 存在检查说明"
layer: L2
status: pass
---

## Test

mock `claude` 命令（捕获 prompt 参数）；以 `EXISTING_BRANCH=feat/REQ-039 FORCE=true bash harness.sh implement REQ-039` 调用；
检查传给 claude 的 prompt。

## Expected

- prompt 含 `gh pr list --head feat/REQ-039` 字样
- prompt 含 "If found" 和 "gh pr edit" 字样
- 不含 `EXISTING_BRANCH` 时（无 TC），prompt 仍含 `gh pr create --fill`
