---
tc_id: TC-039-11
req_id: REQ-039
title: "AGENT_STALL_TIMEOUT_MINUTES 可配置，默认 60"
layer: L2
status: pending
---

## Test

写 runtime/menglan_alive.ts = epoch 45 分钟前；
构造 in_progress REQ；
Case A：`.env` 中 `AGENT_STALL_TIMEOUT_MINUTES=30` → 超时，发 keep-alive；
Case B：AGENT_STALL_TIMEOUT_MINUTES=60（默认）→ 未超时，不发；
Case C：未设 AGENT_STALL_TIMEOUT_MINUTES（使用默认 60）→ 行为同 Case B。

## Expected

- Case A：消息已发，重复运行不重复生成 keep-alive
- Case B、C：消息未发
