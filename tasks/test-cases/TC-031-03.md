---
tc_id: TC-031-03
title: archive_merged_reqs — PR 已 closed（未 merge），不触发归档
req_ref: REQ-031
layer: L1
type: functional
status: ready
---

# Scenario

`archive_merged_reqs()` 检测到 PR 状态为 CLOSED（非 MERGED），不执行归档操作。
（CLOSED 与 MERGED 在 gh API 中是不同的 state 值，需严格区分。）

# Steps

1. 在 tmpdir 创建：
   - `tasks/features/REQ-099.md`（status: review，pr_number: 42，test_case_ref: [TC-099-01]）
   - `tasks/test-cases/TC-099-01.md`（status: ready）
2. 注入 mock `gh`：`gh pr view 42 --json state` 返回 `{"state":"CLOSED"}`
3. 注入 mock `git`：记录调用（期望不被调用）
4. 调用 `archive_merged_reqs()`

# Expected

- `tasks/features/REQ-099.md` 仍存在，status 未变（仍为 review）
- `tasks/test-cases/TC-099-01.md` 仍存在，status 未变
- `tasks/archive/done/REQ-099.md` 不存在
- git commit 未被调用
