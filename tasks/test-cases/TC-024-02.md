---
tc_id: TC-024-02
title: mock getUpdates 返回 start REQ-021，断言 REQ-021 被认领
req_ref: REQ-024
layer: L1
type: functional
status: ready
---

# Scenario

mock Telegram getUpdates 返回 `start REQ-021` 文本消息，
`handle_telegram_commands` 应触发 `auto_claim_specific "REQ-021"`。

# Steps

1. 设置 `TELEGRAM_BOT_TOKEN=mock_token TELEGRAM_CHAT_ID=12345`
2. Mock `curl` 返回：`{"ok":true,"result":[{"update_id":1,"message":{"chat":{"id":12345},"text":"start REQ-021"}}]}`
3. 创建临时 `tasks/features/REQ-021.md`（status=ready, owner=unassigned, tc_policy=optional）
4. 调用 `handle_telegram_commands`
5. 断言 REQ-021 的 owner 变为 `claude_code`

# Expected

- REQ-021 被认领（owner=claude_code）
- offset 文件更新为 2（update_id+1）
