---
tc_id: TC-023-03
title: tc_complete status=success 路由 implement 消息到 inbox/for-menglan/
req_ref: REQ-023
layer: L1
type: functional
status: ready
---

# Scenario

`inbox/for-pandas/` 中存在 `tc_complete` 消息（status=success），
`inbox_read_pandas` 处理后应在 `inbox/for-menglan/` 写出 `implement` 消息。

# Steps

1. 设置临时 `SHARED_RESOURCES_ROOT`
2. 创建 `inbox/for-pandas/2026-03-16-huahua-tc-done-REQ-021.md`：
   `type: tc_complete, req_id: REQ-021, status: success, summary: TC approved`
3. 调用 `inbox_read_pandas`（mock tg_pr_ready/tg_decision）
4. 断言 `inbox/for-menglan/` 下出现 `implement` 类型文件
5. 断言文件 frontmatter 包含 `type: implement`, `req_id: REQ-021`

# Expected

- `for-menglan/` 下存在 implement 消息文件
- 原 `for-pandas/` 消息文件已删除
