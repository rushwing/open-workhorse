---
tc_id: TC-034-05
title: inbox_init 幂等 — 重复调用不报错，目录均存在
req_ref: REQ-034
layer: L1
type: functional
status: ready
---

# Scenario

`inbox_init()` 多次调用不报错，所有 pending/claimed/done/failed 子目录均存在。

# Steps

1. 设置 SHARED_RESOURCES_ROOT 为临时目录
2. 连续调用 `inbox_init` 三次
3. 检查所有三个 agent 下的四个子目录是否存在

# Expected

- 退出码为 0
- 对所有 agent（pandas, huahua, menglan）的 pending/claimed/done/failed 目录均存在
- 无错误输出
