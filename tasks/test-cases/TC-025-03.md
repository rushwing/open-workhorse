---
tc_id: TC-025-03
title: inbox 消息 blocking_reason=depends_on 触发 TRIGGER-002
req_ref: REQ-025
layer: L1
type: functional
status: ready
---

# Scenario

`inbox/for-pandas/` 收到 `major_decision_needed` 消息，
`blocking_reason` 包含 "depends_on"，应触发 TRIGGER-002。

# Steps

1. 创建 `inbox/for-pandas/` 消息：
   `type: major_decision_needed, req_id: REQ-022, blocking_reason: depends_on REQ-021 (unfinished)`
2. Mock `tg_decision`
3. 调用 `inbox_read_pandas`
4. 断言 `tg_decision` 被调用

# Expected

- `tg_decision` 调用消息包含 "REQ-022" 和 "依赖项"
- 原消息文件已被删除
