---
tc_id: TC-039-02
req_id: REQ-039
title: "Pandas _handle_tc_complete 将 branch_name 写入 implement 消息"
layer: L1
status: pending
---

## Test

构造 tc_complete 消息（payload 含 `branch_name: feat/REQ-039`），经 pandas-heartbeat.sh `_handle_tc_complete` 处理后，写入 for-menglan/pending/ 的 implement 消息包含 `branch_name: feat/REQ-039`。

## Expected

- implement 消息文件存在于 `for-menglan/pending/`
- 文件 payload 区 `grep "^branch_name: feat/REQ-039"` 命中
