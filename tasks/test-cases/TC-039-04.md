---
tc_id: TC-039-04
req_id: REQ-039
title: "Menglan implement handler 提取 branch_name 并以 EXISTING_BRANCH 传递给 harness.sh"
layer: L2
status: pass
---

## Test

构造带 `branch_name: feat/REQ-039` 的 implement 消息；mock `harness.sh implement`（捕获环境变量）；
执行 menglan-heartbeat.sh implement handler；
验证 mock 收到 `EXISTING_BRANCH=feat/REQ-039`。

## Expected

- mock harness.sh 被调用
- `EXISTING_BRANCH` 环境变量 = `feat/REQ-039`
- 不含 branch_name 的 implement 消息：`EXISTING_BRANCH` 为空（回归）
