---
tc_id: TC-023-02
title: harness.sh tc-review 有效 PR 调用 Claude（mock claude binary）
req_ref: REQ-023
layer: L1
type: integration
status: ready
---

# Scenario

mock `claude` binary 为记录调用参数的脚本，运行 `harness.sh tc-review 99`，
断言 Claude 被调用且 prompt 包含 TC 评审内容。

# Steps

1. 在临时 PATH 中创建 mock `claude`：`echo CLAUDE_CALLED "$@"; exit 0`
2. Mock `gh` 返回合理假数据
3. 运行 `CLAUDE_APPROVAL=--dangerously-skip-permissions bash scripts/harness.sh tc-review 99`
4. 断言 mock claude 被调用
5. 断言 prompt 包含 "adequate" / "missing-branch" / "redundant" 标签说明

# Expected

- Claude mock 被调用一次
- 传入 prompt 包含 TC review 相关指令
- 退出码 = 0
