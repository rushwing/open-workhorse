---
tc_id: TC-028-04
title: ci_bug 完整主路径（pandas 自动确认，含 REQ blocking/unblocking）
req_ref: REQ-028
layer: L1
type: functional
status: ready
---

# Scenario

ci_bug 从 open 推进到 closed 的完整主路径。confirmed 由 pandas 自动执行（无需人工）；关联 REQ 从 review → blocked → 恢复 review。

# Steps

1. 构造 bug fixture：`bug_type=ci_bug, status=open, related_req=[REQ-001]`；构造 REQ fixture：`status=req_review`
2. 调用自动确认流程（模拟 pandas 触发）；断言 `bug.status=confirmed, bug.owner=pandas`
3. 断言 confirmed 转换由 pandas 自动执行，无需额外人工操作
4. 断言 `REQ-001.status=blocked, REQ-001.blocked_reason=bug_linked, REQ-001.blocked_from_status=req_review`
5. 调用 transition(confirmed → in_progress)；断言 `bug.owner=menglan`
6. 调用 transition(in_progress → fixed)；断言 `bug.status=fixed`
7. 调用 transition(fixed → regressing)；断言 `bug.status=regressing`
8. 调用 transition(regressing → closed)；断言 `bug.status=closed`
9. 断言 `REQ-001.status=req_review`（恢复 blocked_from_status）
10. 断言 `REQ-001.blocked_reason` 字段为空或不存在

# Expected

- ci_bug 触发者=pandas，confirmed 由 pandas 自动执行（区别于其他 bug 类型的手动确认）
- fix 责任人=menglan
- REQ unblock 恢复至 req_review
- blocked_reason 和 blocked_from_status 字段清空
