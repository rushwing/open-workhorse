---
tc_id: TC-033-10
title: inbox_read_pandas 读旧格式（type=dev_complete）→ 旧 handler 正常处理
req_ref: REQ-033
layer: L1
type: functional
status: ready
---

# Scenario

`inbox_read_pandas()` 能兼容读取旧格式 `dev_complete` 消息，
通过 `_inbox_read_legacy()` 路由到 `_handle_dev_complete`。

# Steps

1. 在 `for-pandas/` 写入旧格式消息：
   ```yaml
   ---
   type: dev_complete
   req_id: REQ-033
   pr_number: 99
   summary: 实现完成
   status: success
   ---
   ```
2. 调用 `inbox_read_pandas`（mock tg_pr_ready）

# Expected

- exit code 0
- `tg_pr_ready` 被调用（dev_complete success）
- 消费后消息文件被删除
