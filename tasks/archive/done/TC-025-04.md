---
tc_id: TC-025-04
title: inbox 消息 blocking_reason=outside REQ boundary 触发 TRIGGER-003
req_ref: REQ-025
layer: L1
type: functional
status: ready
---

# Scenario

inbox 消息 `blocking_reason` 包含 "implementation requires changes outside REQ boundary"，
应触发 TRIGGER-003（范围扩展）。

# Steps

1. 创建 `inbox/for-pandas/` 消息：
   `type: major_decision_needed, req_id: REQ-022, blocking_reason: implementation requires changes outside REQ boundary`
2. Mock `tg_decision`
3. 调用 `inbox_read_pandas`
4. 断言 `tg_decision` 被调用，消息含 "Approve" 和 "Constrain" 选项

# Expected

- TRIGGER-003 触发
- `tg_decision` 选项为 "Approve" / "Constrain"
