---
tc_id: TC-039-07
req_id: REQ-039
title: "tc_policy=exempt 回归：无 branch_name，harness 正常新建分支和 PR"
layer: L2
status: pass
---

## Test

构造 tc_policy=exempt 的 implement 消息（不含 branch_name）；
mock harness.sh implement（捕获环境变量和 prompt）；
执行 menglan-heartbeat.sh implement handler。

## Expected

- `EXISTING_BRANCH` 环境变量为空（或未设置）
- harness.sh 照常调用（不受影响）
- prompt 中含 `gh pr create --fill`（非 edit 路径）
