---
tc_id: TC-039-05
req_id: REQ-039
title: "harness.sh cmd_worktree_setup：远端分支已存在时 fetch+checkout，不新建"
layer: L2
status: pending
---

## Test

在临时 git repo 中，pre-create 远端 `feat/REQ-039` 分支（有一次额外提交）；
调用 `harness.sh implement REQ-039`（或直接调 `cmd_worktree_setup`）；
验证 worktree 分支来自远端，包含那次额外提交。

## Expected

- worktree 建立成功
- 分支 HEAD 包含远端的额外提交（非基于 origin/main 新建）
- 输出含 "分支已从远端拉取" 字样
