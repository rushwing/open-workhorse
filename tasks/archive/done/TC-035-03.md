---
tc_id: TC-035-03
title: Response correlation_id 匹配 → handler 正常执行，消息进 done/
req_ref: REQ-035
layer: L1
type: functional
status: ready
---

# Scenario

Pandas 在 for-menglan/done/ 中有对应 request（correlation_id=X），收到 response 也携带 X。

# Steps

1. 在 `for-menglan/done/` 预置 type=request 消息（correlation_id=X，req_id=REQ-035）
2. 在 `for-pandas/pending/` 写入 type=response（correlation_id=X，legacy_type=dev_complete）
3. 调用 `inbox_read_pandas`

# Expected

- response 移到 `for-pandas/done/`
- 退出码为 0
- 无 warn/error 日志
