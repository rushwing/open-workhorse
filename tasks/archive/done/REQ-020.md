---
req_id: REQ-020
title: Pandas 编排循环基础设施（IPC + heartbeat + TC委派 + TG + 决策升级）
status: done
priority: P1
phase: phase-2
owner: claude_code
depends_on: [REQ-021, REQ-022, REQ-023, REQ-024, REQ-025]
test_case_ref: []
tc_policy: exempt
tc_exempt_reason: umbrella REQ — acceptance delegated to child REQs REQ-021..025
scope: scripts
acceptance: REQ-021..025 全部达到 done 状态（基础设施层）；npm run build pass；npm run release:audit pass；npm run req:check pass；119 个测试全部通过。注：umbrella 自动晋级与 E2E 端到端闭环由 REQ-026 承接，不在本 REQ 验收范围内
---

# Goal

为 open-workhorse 建立 Pandas 编排循环**基础设施**：共享收件箱 IPC、heartbeat 任务调度、TC 设计委派（Huahua→Menglan）、Telegram 双向通信、重大决策升级。

> **范围边界**：本 REQ 覆盖"基础设施能运行"这一层。umbrella REQ 自动晋级（子 PR merge 检测→批量 done→准出核查→Daniel 合并通知）端到端闭环由 **REQ-026** 承接；REQ-026 完成前 umbrella routing 仍处于 dead-letter 状态，属已知缺口。

设计细节见 `docs/PANDAS-ORCHESTRATION.md`。

# In Scope

本 REQ 是 umbrella，以下为子需求范围：

- **REQ-021** — 共享收件箱 IPC 集成（inbox 读写、消息格式、polling）
- **REQ-022** — Pandas 心跳任务（OpenClaw APP_COMMAND=pandas-heartbeat 集成）
- **REQ-023** — TC 设计闭环（Huahua→Menglan，harness.sh tc-review，≤2轮）
- **REQ-024** — 双向 Telegram（Daniel→Pandas 文本指令解析）
- **REQ-025** — 重大决策检测与 Telegram 升级

# Out of Scope

- 具体 agent 实现（Huahua、Menglan 各自的实现逻辑）
- UI 可视化

# Acceptance Criteria

- REQ-021、REQ-022、REQ-023、REQ-024、REQ-025 全部 status=done
- `npm run build` 通过
- `npm run release:audit` 通过
- `npm run req:check` 通过
- 119 个测试全部通过（覆盖 TC-021-01..04、TC-022-01..04、TC-023-01..05、TC-024-01..05、TC-025-01..05）

> 端到端完整循环验证（umbrella 自动晋级、PR 合入检测、Menglan 准出核查）由 REQ-026 承接。

# Test Case Design Notes

无独立 TC（tc_policy=exempt）。验收以子 REQ 测试 + 质量门禁为准。

# Agent Notes

## 实现摘要（2026-03-16）

本 PR 包含 REQ-020 umbrella 的完整实现：

**新增 REQ 文件：** REQ-021、REQ-022、REQ-023、REQ-024、REQ-025（均 status=review）

**已实现的脚本：**
- `scripts/pandas-heartbeat.sh` — inbox IPC、auto_claim、stall_detection、TRIGGER-001/002/003（REQ-021/022/025）
- `scripts/telegram.sh` — tg_notify、tg_poll_commands、tg_decision、tg_pr_ready（REQ-024）
- `scripts/harness.sh` — tc-review 子命令（REQ-023）
- `src/index.ts` — APP_COMMAND=pandas-heartbeat 分发（REQ-022）

**测试（`test/pandas-heartbeat.test.ts`）：** 23 个测试全部通过，覆盖 TC-021-01..04、TC-022-01..04、TC-023-01..05、TC-024-01..05、TC-025-01..05

**质量门禁：** `npm run release:audit` pass、`npm run build` pass、`npm run req:check` pass（25 REQs，0 orphan TC）

---

> 原端到端 smoke 步骤已移至 REQ-026（Umbrella REQ 自动晋级与端到端编排闭环）。
