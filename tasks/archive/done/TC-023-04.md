---
tc_id: TC-023-04
title: tc_complete status=blocked iteration=1 路由修复请求到 inbox/for-huahua/
req_ref: REQ-023
layer: L1
type: functional
status: ready
---

# Scenario

`tc_complete` 消息 status=blocked，iteration=1（首轮），
应将修复请求路由到 `inbox/for-huahua/`（iter ≤ 2 阈值）。

# Steps

1. 创建 `inbox/for-pandas/` 消息：
   `type: tc_complete, req_id: REQ-021, status: blocked, blocking_reason: missing coverage, iteration: 1`
2. 调用 `inbox_read_pandas`
3. 断言 `inbox/for-huahua/` 下出现消息文件
4. 断言文件 frontmatter 包含 `type: tc_design`, `iteration: 2`

# Expected

- `for-huahua/` 下存在 tc_design 修复消息
- iteration 字段递增至 2
- 未调用 `tg_decision`
