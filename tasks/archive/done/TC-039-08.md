---
tc_id: TC-039-08
req_id: REQ-039
title: "menglan-heartbeat 每次运行写 runtime/menglan_alive.ts"
layer: L2
status: pending
---

## Test

以空 inbox 调用 menglan-heartbeat.sh（会 exit 0 但仍写时间戳）；
检查 `runtime/menglan_alive.ts` 是否存在且内容为合理 epoch 整数。

## Expected

- 文件存在
- 内容为 10 位 epoch 整数（`[0-9]{10}`）
- 多次调用后文件内容更新（时间戳递增）
