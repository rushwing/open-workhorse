---
tc_id: TC-035-01
title: 首次 inbox_write 创建 thread_id，写入 Envelope，不修改 REQ 文件
req_ref: REQ-035
layer: L1
type: functional
status: ready
---

# Scenario

首次为某 REQ 调用 `inbox_write()`，无任何既有消息轨迹。

# Steps

1. 设置 SHARED_RESOURCES_ROOT 为临时目录（无既有消息）
2. 调用 `inbox_write "menglan" "implement" "REQ-035" "test"`
3. 读取生成的消息文件

# Expected

- `for-menglan/pending/` 下有一个 .md 文件
- 文件 frontmatter 含 `thread_id: thread_REQ-035_` 前缀
- 文件 frontmatter 含 `correlation_id: corr_REQ-035_` 前缀
- `tasks/features/REQ-035.md` 内容与调用前完全一致（thread_id 不写回 REQ 文件）
