---
req_id: REQ-020
title: Pandas 完整编排循环（umbrella）
status: draft
priority: P1
phase: phase-2
owner: unassigned
depends_on: [REQ-021, REQ-022, REQ-023, REQ-024, REQ-025]
test_case_ref: []
tc_policy: exempt
tc_exempt_reason: umbrella REQ — acceptance delegated to child REQs REQ-021..025
scope: scripts
acceptance: REQ-021..025 全部达到 done 状态；端到端 smoke：Pandas 从 IDLE 经 TC_DESIGN → DEV_ACTIVE → CODE_REVIEW → PR_READY 完成一个测试 REQ 的完整循环，Daniel 收到 tg_pr_ready 通知
---

# Goal

为 open-workhorse 建立 Pandas 完整编排循环：从任务认领、TC 设计委派、实现委派、代码评审，到向 Daniel 发送合并通知，形成闭合的自动化循环。

设计细节见 `docs/PANDAS-ORCHESTRATION.md`。

# In Scope

本 REQ 是 umbrella，以下为子需求范围：

- **REQ-021** — 共享收件箱 IPC 集成（inbox 读写、消息格式、polling）
- **REQ-022** — Pandas 心跳任务（OpenClaw APP_COMMAND=pandas-heartbeat 集成）
- **REQ-023** — TC 设计闭环（Huahua→Menglan，harness.sh tc-review，≤2轮）
- **REQ-024** — 双向 Telegram（Daniel→Pandas 文本指令解析）
- **REQ-025** — 重大决策检测与 Telegram 升级

# Out of Scope

- 具体 agent 实现（Huahua、Menglan 各自的实现逻辑）
- UI 可视化

# Acceptance Criteria

- REQ-021、REQ-022、REQ-023、REQ-024、REQ-025 全部 status=done
- `npm run req:check` 通过
- 端到端 smoke 测试可手动执行（见 Agent Notes）

# Test Case Design Notes

无独立 TC（tc_policy=exempt）。端到端 smoke 由 Agent Notes 描述。

# Agent Notes

端到端 smoke 步骤（手动，需 Telegram 配置）：
1. 创建一个最小 REQ-999 测试需求（status=ready, tc_policy=exempt）
2. 触发 pandas-heartbeat：`APP_COMMAND=pandas-heartbeat npm run dev`
3. 观察：inbox/for-huahua/ 出现 tc_design 消息（tc_policy=exempt 时跳过）→ inbox/for-menglan/ 出现 implement 消息
4. Menglan 完成后写入 inbox/for-pandas/: dev_complete REQ-999
5. 验证：Daniel 收到 tg_pr_ready Telegram 通知
6. 清理 REQ-999
