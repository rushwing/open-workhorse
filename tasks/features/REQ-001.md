---
req_id: REQ-001
title: PHASE-000 exit criteria 回填
status: draft
priority: P0
phase: phase-0
owner: unassigned
depends_on: []
test_case_ref: []
tc_policy: exempt
tc_exempt_reason: docs-only change; no executable code to test
scope: docs
acceptance: PHASE-000.md 所有 exit criteria checkboxes 勾选完毕，且至少一个 REQ 从 draft 走到 done
---

# Goal

回填 PHASE-000.md 中的 exit criteria checkboxes，将所有已满足的条件标记为完成，使 Phase 0 正式退出。同时本 REQ 本身走完完整 harness 流程（draft → done），满足"至少一个 REQ 从 draft 走到 done"的退出条件。

# In Scope

- 更新 `tasks/phases/PHASE-000.md` 中所有 exit criteria 的 checkbox 状态
- 验证各 exit criteria 对应实物均已存在（CI workflow、harness 文档、scripts、tasks/ 骨架）
- 将本 REQ 状态推进至 done，完成 Phase 0 的 harness 流程验证

# Out of Scope

- 修复任何尚未满足的 exit criteria（应在单独 REQ 中处理）
- Phase 1 工作项

# Acceptance Criteria

- PHASE-000.md 中全部 6 项 exit criteria 均已打勾（`[x]`）
- `bash scripts/check-req-coverage.sh` exit 0
- 本 REQ 状态为 done，已归档至 `tasks/archive/done/`

# Test Case Design Notes

豁免测试用例设计（tc_policy=exempt）：本需求为纯文档更新，无执行逻辑，人工 review 即可验收。

# Agent Notes

实现前确认：
1. `tasks/phases/PHASE-000.md` 当前哪些 checkboxes 未勾选
2. 逐一核实对应实物是否存在（如 `.github/workflows/ci.yml`、`harness/` 目录等）
3. 只更新已满足的条件；如有未满足项，在此处记录并保持 unchecked
