---
req_id: REQ-009
title: LLM ReAct Deploy — open-workhorse 克隆 + 依赖初始化
status: draft
priority: P2
phase: phase-2
owner: unassigned
depends_on: [REQ-008]
test_case_ref: []
tc_policy: required
tc_exempt_reason: ""
scope: scripts
acceptance: Pi 上 git clone rushwing/open-workhorse 成功，npm install 通过，无 fatal 错误
---

# Goal

通过 LLM ReAct 循环（Claude Code CLI 或其他 LLM via SSH）在 Pi 上克隆 open-workhorse 仓库并完成 Node.js 依赖初始化，为后续服务配置和注册做准备。

# In Scope

- LLM ReAct Agent 通过 Tailscale SSH 连接 Pi
- `git clone https://github.com/rushwing/open-workhorse.git`（通过 mihomo 代理）
- `npm install`（通过代理下载 npm 依赖）
- 验证 `node_modules/` 存在，关键依赖安装成功

# Out of Scope

- `.env` 配置（REQ-010 覆盖）
- openclaw binary 安装（REQ-010 覆盖）
- systemd service 注册（REQ-011 覆盖）
- LLM ReAct 调用方式的具体实现（在 ready 阶段与 Daniel+CodeX 确定）

# Acceptance Criteria

- `/home/openclaw/open-workhorse/` 目录存在，含完整仓库文件
- `npm install` 退出码为 0
- `npm run build` 退出码为 0（确认 TypeScript 编译通过）

# Test Case Design Notes

需要覆盖的场景：
- TC: git clone 通过代理成功（包含 .git 目录，非空仓库）
- TC: npm install 成功（node_modules 存在，无 peer dep error）
- TC: npm run build 成功（dist/ 目录生成）

注意：L3 硬件集成测试。LLM ReAct 调用方式（Claude Code CLI / Anthropic API）在 ready 阶段确认。

# Agent Notes

- 依赖 REQ-008（Tailscale SSH 可达）
- 服务用户为 `openclaw`（由 auto-mihomo 创建，REQ-007 保证）
- npm 代理配置可能需要设置 `npm config set proxy http://127.0.0.1:7890`
- LLM ReAct 循环的具体方案（Claude Code CLI / API）待 Daniel+CodeX 在 ready 阶段确认后填入 Agent Notes
