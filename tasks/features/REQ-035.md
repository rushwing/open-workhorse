---
req_id: REQ-035
title: "ATM-P1b — Thread & Correlation 追踪（thread_id / correlation_id）"
status: review_ready
priority: P1
phase: phase-3
owner: unassigned
blocked_reason: ""
blocked_from_status: ""
blocked_from_owner: ""
depends_on: [REQ-033, REQ-034]
test_case_ref: []
tc_policy: required
tc_exempt_reason: ""
scope: scripts
acceptance: >
  Pandas 发出首个 request 时创建 thread_id 并写入 REQ Agent Notes；
  同一 REQ 后续所有往返消息携带相同 thread_id；
  response 携带对应 correlation_id；
  correlation_id 不匹配时 warn + 消息移到 failed/；
  npm test 通过；npm run build 通过
pending_bugs: []
---

# Goal

在 REQ-033 Envelope 基础上，落实 Thread / Correlation 的创建与传播规则，
使一个 REQ 的完整协作链路（Pandas↔Menglan↔Huahua 多轮往返）可以被追踪和重建。

# In Scope

- `scripts/pandas-heartbeat.sh`：`auto_claim()` 在路由第一个 request 时创建 `thread_id`
  - 格式：`thread_{req_id}_{epoch}`
  - 持久化：将 `thread_id` 写入 REQ 文件的 Agent Notes
- `scripts/pandas-heartbeat.sh`：每次发出新 request 复用已有 `thread_id`，创建新 `correlation_id`
- `scripts/pandas-heartbeat.sh`：处理 response 时，提取 `correlation_id`，确认与发出的 request 配对
  - 配对失败 → warn + 写入 `failed/`
- 新增辅助函数 `thread_get_or_create <req_id>`
- 新增辅助函数 `correlation_new <req_id>`

# Out of Scope

- UI 层 thread 链路可视化
- 跨 Team 的 thread

# Acceptance Criteria

- `auto_claim()` 路由第一个 request 时，REQ 文件 Agent Notes 中出现 `thread_id: thread_{req_id}_{epoch}`
- 后续同一 REQ 的所有 request 携带相同 `thread_id`，不同 `correlation_id`
- correlation_id 不匹配时 → warn 日志 + 消息移到 failed/
- `thread_get_or_create` 函数幂等
- `npm run build` 通过
- `npm test` 通过，覆盖 thread 创建、复用、correlation 校验场景

# Test Case Design Notes

需覆盖以下场景：

- TC-035-01：首次 request — thread_id 创建，写入 REQ Agent Notes
- TC-035-02：第二次 request（同一 REQ）— 复用已有 thread_id，新 correlation_id
- TC-035-03：response 配对 — correlation_id 匹配 → handler 正常执行
- TC-035-04：response correlation_id 不匹配 → warn + 消息移到 failed/
- TC-035-05：`thread_get_or_create` 幂等性 — 两次调用返回相同值
- TC-035-06：完整链路重建 — 给定 thread_id，在 done/ 中 grep 可找到所有相关消息

# Agent Notes

参考实现：`~/github-kb/rushwing/knowledge-topics/agent-teams-messaging/REQ-034.md`

## Open Question（待 Daniel 拍板）

**thread_id 持久化位置**：本 REQ In Scope 当前写的是"将 thread_id 写入 REQ 文件的 Agent Notes"。
这样做有可追踪性优势，但也会把一次运行的临时链路 ID 写回 source-of-truth 工作项，
重试/重跑时可能出现多个 thread_id 并存、语义不清的问题。

两个可选方向：
- **A）写回 REQ 文件**（当前方案）— 链路 ID 和工作项绑定，便于人工查找；但 REQ 文件会被运行时状态污染
- **B）只留在 done/ 轨迹里**（inbox done/ 消息文件里的 thread_id 字段自然构成链路）— REQ 文件保持纯规格，可追踪性依赖消息文件

请 Daniel 在实施 REQ-035 前确认选 A 还是 B，实施者按决定调整 In Scope 和验收标准。
