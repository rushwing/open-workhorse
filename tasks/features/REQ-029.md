---
req_id: REQ-029
title: 需求流转状态机 v0.4 — review_ready + pending_bugs + 流程可视化 DAG
status: test_designed
priority: P1
phase: phase-2
owner: unassigned
blocked_reason: ""
blocked_from_status: ""
depends_on: [REQ-027]
test_case_ref: [TC-029-01, TC-029-02, TC-029-03, TC-029-04, TC-029-05, TC-029-06, TC-029-07, TC-029-08, TC-029-09]
tc_policy: required
tc_exempt_reason: ""
scope: docs
acceptance: |
  requirement-standard.md v0.4 含 review_ready + pending_bugs；check-req-coverage.sh 校验通过（STATUS_ENUM 含 review_ready，pending_bugs 引用存在性校验）；docs/req-flow.html 含 9 个状态节点
pending_bugs: []
---

# Goal

将本次对话中 Daniel 与 Claude Code 对齐的需求流转 DAG 结论写入规程 v0.4，并可视化全状态机。

核心对齐结论：

1. **`review_ready` 新状态**：Human-to-Pipeline 交接点。Daniel 通过 TG/UI 将 REQ 设为 `review_ready`；Pandas 只扫描此状态，原子 commit 转为 `req_review`（state-as-lock，防重复）
2. **`pending_bugs: []` 新 frontmatter 字段**：结构化路由信号（替代解析 Agent Notes 自由文本）。Menglan routing 规则：status=test_designed → tc_review；status=req_review + pending_bugs 非空 → fix req_bug
3. **`blocked` 保持 "Human needed" 语义**，不用于 agent-to-agent 的正常流转打回
4. **Huahua happy path**：req_review 阶段同步做 TC 设计，直接 → test_designed（跳过 ready 中间态，规程需明确允许此折叠）
5. **tc_review FAIL 状态**：仍为 test_designed（非新状态），Huahua 修 TC 后 inbox → Menglan 二次确认

# In Scope

1. `harness/requirement-standard.md` → v0.4
   - §2.4：状态总数 8 → 9，新增 `review_ready`
   - §5.1：新增 `pending_bugs: []` 字段（可选，BUG-xxx 数组；非空时为 Menglan 路由信号）
   - §5.2：模板加 `pending_bugs: []`
   - §6.1：新增 `review_ready` 状态定义
   - §6.2：新增 `draft → review_ready`（Human 设置）、`review_ready → req_review`（Pandas state-as-lock claim）合法流转；明确允许 req_review → test_designed 折叠（Huahua 同步做 TC 设计）
   - §6.3：非法流转补充（不允许 Pandas 直接 claim `draft`，must go through `review_ready`）
   - §8.4：Pandas claim 规程更新——扫描 `review_ready`，单 commit 改 status + owner 即锁定
   - 新增 §Menglan 路由规则：status=test_designed → tc_review；status=req_review + pending_bugs 非空 → fix req_bug

2. `scripts/check-req-coverage.sh`
   - STATUS_ENUM 加入 `review_ready`
   - 新增 pending_bugs 引用存在性校验（类似 depends_on 逻辑，检查 tasks/bugs/BUG-xxx.md）

3. `docs/req-flow.html`（新文件）
   - 可视化 HTML+CSS 泳道图 + DAG，含全部 9 个状态节点
   - 标注各节点责任 Agent（Pandas/Huahua/Menglan/Daniel）
   - 标注关键字段（review_ready、pending_bugs、state-as-lock）
   - 不依赖外部 CDN，纯 HTML+CSS+SVG

# Out of Scope

- 实际 TypeScript 状态机实现代码
- UI 集成测试
- CI pipeline 配置变更

# Acceptance Criteria

- `harness/requirement-standard.md` 版本号更新至 v0.4，含 `review_ready` 状态和 `pending_bugs` 字段
- `scripts/check-req-coverage.sh` STATUS_ENUM 含 `review_ready`，含 pending_bugs 存在性校验
- `docs/req-flow.html` 存在，含 9 个状态节点关键词
- `npm run req:check` exit 0

# Agent Notes

## 认领记录

- 2026-03-17：daniel 设计需求，claude_code 完成 TC 设计（TC-029-01 ～ TC-029-09）
